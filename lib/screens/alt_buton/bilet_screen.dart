import 'package:flutter/material.dart';

class BiletlerSayfasi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Biletler')),
      body: Center(child: Text('Biletleriniz Burada')),
    );
  }
}
