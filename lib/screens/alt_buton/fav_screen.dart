import 'package:flutter/material.dart';

class FavorilerSayfasi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Favoriler')),
      body: Center(child: Text('Favori Filmleriniz Burada')),
    );
  }
}
