import 'package:flutter/material.dart';

class FilmListScreen extends StatelessWidget {
  const FilmListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Film Listesi'));
  }
}
